CREATE TABLE createAccount (
    id INT NOT NULL AUTO_INCREMENT,
    firstName VARCHAR(50) NOT NULL,
    lastName VARCHAR(50) NOT NULL,
    email VARCHAR NOT NULL,
    openSesame VARCHAR(20) NOT NULL,
    PRIMARY KEY(id)
)